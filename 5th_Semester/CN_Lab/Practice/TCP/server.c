#include <stdio.h> 
/*for scanf, printf, gets and close
    standard input/output functions */
#include <stdlib.h>
/*for exit() function */
#include <sys/types.h>
/*for socket(), bind(), listen(), accept(), recv() and send() */
#include <sys/socket.h>
/*for socket(), bind(), listen(), accept(), recv() and send() 
    The sys/socket.h header file contains sockets definitions.*/
#include <netinet/in.h>
/*for struct sockaddr_in and htons() */
#include <arpa/inet.h>
/*for inet_addr() */
#include <fcntl.h>
/*for O_NONBLOCK */
#include <string.h>
/*for strlen() */

int main(void)
{
    
}
