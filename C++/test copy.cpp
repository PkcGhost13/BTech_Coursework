#include <iostream>
#include <stdio.h>
#define MAX 100

using namespace std;

int main(void)
{
    FILE *fp;
    char name[MAX];
    int x,y;
    char o='+';
    //scanf("%d%c%d",&x,&o,&y);
    printf("%d",10^5);
    /*
    printf("Enter filename: ");
    cin>>name;

    if ((fp = fopen(name, "w")) == 0)
        printf("File cannot be opened!");
       printf("%d\n%c\n%d",x,o,y);
    return 0;
    */
}